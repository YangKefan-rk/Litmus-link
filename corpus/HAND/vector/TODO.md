# Vector HAND TODO

- Vector fault with `vstart` and `vl` observation.
- FOF element0 fault versus non-zero element trimming.
- Vector store partial update in idempotent memory after synchronous trap.
- Vector access to PBMT=IO or non-idempotent memory with precise trap policy.
- Segment access trap where `vstart` reports segment index.
