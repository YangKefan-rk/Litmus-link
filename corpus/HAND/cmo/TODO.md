# CMO HAND TODO

- `cbo.clean/flush` with CBCFE denied.
- `cbo.inval` with CBIE trap mode and flush mode.
- `cbo.zero` with CBZE denied.
- CBO address translation page fault and access fault.
- CBO interactions with non-coherent agents if the platform exposes such agents.
